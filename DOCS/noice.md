# Noice

This plugin provides a cleaner and more modern UI for Neovim messages, cmdline, and the popupmenu.

## Configuration

The configuration for `noice` is located in `lua/plugins/noice.lua`.

## Keybindings

