# Tmux Navigator

This plugin allows seamless navigation between Neovim panes and tmux panes.

## Configuration

The configuration for `tmux-navigator` is located in `lua/plugins/tmux-navigator.lua`.

## Keybindings

